# Maquette Ohm Sweet Ohm

## Install 
 - npm init
 - npm install tailwindcss @tailwindcss/cli
 - add this to package.json scripts : "dev": npx @tailwindcss/cli -i assets/lib/tailwind.css -o assets/css/style.css --watch
 - npm run dev
